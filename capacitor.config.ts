import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreeva.indiatourscabs',
  appName: 'IndiaTourCabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
